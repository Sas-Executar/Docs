# Vendor

O diretório `anthropic-knowledge-work-plugins/` é um submódulo do upstream oficial `anthropics/knowledge-work-plugins` e deve ser tratado como read-only.

Use `git submodule update --init --recursive` ou `bash scripts/bootstrap.sh`.
