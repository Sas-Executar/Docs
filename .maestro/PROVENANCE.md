# Provenance

Distinguir sempre: fonte do usuário, documento canônico, código de referência, workflow/skill Anthropic, pesquisa externa e inferência.

Metadata mínima: source, source_type, timestamp, primary_domain, skills_used, destination, classification, conflicts e assumptions.

O conteúdo em `vendor/anthropic-knowledge-work-plugins/` é read-only e preserva licença/atribuição upstream.
